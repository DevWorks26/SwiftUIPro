    import XCTest
    @testable import SwiftUIPro

    final class SwiftUIProTests: XCTestCase {
        func testExample() {
            // This is an example of a functional test case.
            // Use XCTAssert and related functions to verify your tests produce the correct
            // results.
            XCTAssertEqual(SwiftUIPro().text, "Hello, World!")
        }
    }
